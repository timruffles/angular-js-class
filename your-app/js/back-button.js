app.directive("backButton",function(backState,$location) {
  /*
   * should need replace, link, scope, template
   * */
  return {
    // config!
  };
});

app.factory("backState",function() {
  return function(name,route) {
    // getter/update
  };
});
